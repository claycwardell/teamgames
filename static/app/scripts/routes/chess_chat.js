/*global define*/

define([
    'jquery',
    'backbone'
], function ($, Backbone) {
    'use strict';

    var ChessChatRouter = Backbone.Router.extend({
        routes: {
        }

    });

    return ChessChatRouter;
});